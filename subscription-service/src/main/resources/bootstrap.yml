spring:
  profiles: docker
  cloud:
    consul:
      host: consul-tenflix
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${random.value}
      enabled: true
eureka:
  client:
    enabled: false